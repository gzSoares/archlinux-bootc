# pacotes_necessarios
# Optional packages installed via pacman at build time.
# Official Arch repos only (core, extra, multilib).
# AUR packages must be installed at runtime.
# One package per line. Lines starting with # are ignored.

# Development tools
buildah
podman

# Extra fonts
ttf-fira-code

# Network utilities
openssh

# Additional compression
unrar
